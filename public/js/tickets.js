let eventsSelect = document.querySelector("#events-multi-select"); const events = (() => { let e = null; return $.ajax({ async: !1, global: !1, url: "../util/events.json", dataType: "json", success: t => { e = t } }), e })(); events.forEach(e => { eventsSelect.innerHTML += `<option class="event-dropdown-item">$${e.price} - ${e.date} - ${e.teamOne} VS ${e.teamTwo} (Football Match)</option>` }); let form = document.getElementById("tickets-form"); function calcTicketsPrice() { let e = document.querySelector("#events-multi-select option:checked").value, t = document.querySelector("#tickets-quantity option:checked").value, n = document.getElementById("calculated-price"), c = parseFloat(e.substring(1, 6)), l = parseInt(t); n.value = `$${(l * c).toFixed(2)}` } form.addEventListener("click", e => { e.preventDefault() }), form.addEventListener("submit", e => { e.preventDefault() });